import type { Bitrix24TaskData } from "@/types/bitrix24"
import type { GitLabCommitsData } from "@/types/gitlab"

export function createReportSystemPrompt(): string {
  return `Ты - профессиональный ассистент по составлению технических отчетов для клиентов. Твоя задача - создать профессиональный отчет о проделанной работе на основе предоставленных данных задачи.

СТРОГИЕ ПРАВИЛА ОФОРМЛЕНИЯ:
1. Используй ТОЛЬКО теги <ul> и <li> для всего отчета
2. Никаких других HTML элементов (div, p, strong, em и т.д.)
3. Никаких стилей или атрибутов в тегах
4. Формат: <li>Текст пункта</li>

ПРАВИЛА НАПИСАНИЯ ТЕКСТА:
1. Причастия в краткой форме: "установлен", "размещен", "выполнен", "создан", "настроен"
2. ЗАПРЕЩЕНО: глаголы в прошедшем времени и множественном числе ("установили", "выполнили")
3. Добавляй количественные метрики где возможно (количество страниц, модулей, часов)
4. Группируй похожие действия, избегай дублирования
5. Деловой стиль с технической терминологией

СТОП-СЛОВА (исключить полностью):
- изучение задачи
- погружение в задачу
- оценка
- заданы необходимые вопросы
- обсуждение
- анализ
- рассмотрение

ПРИМЕРЫ ПРАВИЛЬНОГО ОФОРМЛЕНИЯ:
ВЕРНО:
<ul>
  <li>Установлен код пикселя на страницах сайта</li>
  <li>Проверен скрипт валидации форм</li>
  <li>Отрисован макет главной страницы</li>
  <li>Выполнена оптимизация 110 страниц раздела каталога</li>
  <li>Размещено 20 отзывов на различных площадках в интернете</li>
  <li>Протестирована работоспособность выполненных доработок</li>
</ul>

НЕВЕРНО:
<ul>
  <li>Мы установили код пикселя</li>
  <li>Проведено изучение технического задания</li>
  <li><b>Установлен</b> код (использование форматирования)</li>
</ul>

ВАЖНО:
- Только чистый <ul><li> список без каких-либо других элементов
- Текст должен быть объемным и информативным
- Преобразуй техническую информацию в понятный для клиента формат
- Не дублируй информацию, а структурируй и углубляй ее`
}

export function createReportUserPrompt(
  taskData: Bitrix24TaskData,
  commitsData?: GitLabCommitsData | null,
): string {
  let prompt = `Создайте отчет для заказчика на основе следующих данных задачи Битрикс24:\n\n`

  prompt += `**ID ЗАДАЧИ:** ${taskData.taskId || "N/A"}\n`
  prompt += `**ЗАГОЛОВОК:** ${taskData.taskTitle || taskData.title || "Без заголовка"}\n`
  prompt += `**ДАТА ПАРСИНГА:** ${new Date(taskData.timestamp).toLocaleString("ru-RU")}\n\n`

  if (taskData.tabs && Object.keys(taskData.tabs).length > 0) {
    prompt += `**ДАННЫЕ ЗАДАЧИ ПО РАЗДЕЛАМ:**\n\n`

    for (const [tabName, tabData] of Object.entries(taskData.tabs)) {
      prompt += `--- ${tabName.toUpperCase()} ---\n`
      prompt += `${tabData.content || "Нет данных"}\n\n`
    }
  }

  if (commitsData && commitsData.commits.length > 0) {
    prompt += `**ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ О КОММИТАХ:**\n\n`
    prompt += `Ветка: ${commitsData.branch}\n`
    prompt += `Всего коммитов: ${commitsData.totalCount}\n`
    prompt += `Информация о правках в кодовой базе:\n`
    prompt += `${commitsData.commitsText}\n\n`

    if (commitsData.repository.url) {
      prompt += `**РЕПОЗИТОРИЙ:** ${commitsData.repository.url}\n\n`
    }
  }

  prompt += `\n**ЗАДАНИЕ:** На основе этих данных создайте подробный, структурированный отчет для заказчика о выполненных работах. Если предоставлена информация о коммитах, используйте её для более детального описания технических правок в коде. Применяйте все правила оформления указанные в системном промпте (только <ul> и <li> теги, причастия в краткой форме, деловой стиль).`

  return prompt
}

export function createEstimateSystemPrompt(): string {
  return `Ты - опытный технический эксперт по оценке трудозатрат задач веб-разработки. Твоя задача - проанализировать описание задачи и дать обоснованную, декомпозированную оценку времени реализации в часах.

ПРАВИЛА ОЦЕНКИ:
1. Разбей задачу на подзадачи (декомпозиция)
2. Для каждой подзадачи укажи ожидаемое время в часах
3. Укажи риски и неопределенности, которые могут повлиять на оценку
4. Учитывай время на тестирование и код-ревью
5. Дай итоговую оценку: оптимистичную, реалистичную и пессимистичную

ФОРМАТ ОТВЕТА:
- Общая сводка задачи (2-3 предложения)
- Декомпозиция на подзадачи с оценкой времени
- Риски и допущения
- Итоговая оценка: мин / реально / макс (в часах)
- Рекомендации по уточнению оценки

ВАЖНО:
- Будь реалистичным в оценках
- Учитывай скрытые работы (настройка окружения, деплой, тестирование)
- Если данных недостаточно - явно укажи, что нужно уточнить`
}

export function createEstimateUserPrompt(taskData: Bitrix24TaskData): string {
  let prompt = `Оцените трудозатраты для реализации следующей задачи Битрикс24:\n\n`

  prompt += `**ID ЗАДАЧИ:** ${taskData.taskId || "N/A"}\n`
  prompt += `**ЗАГОЛОВОК:** ${taskData.taskTitle || taskData.title || "Без заголовка"}\n`
  if (taskData.tabs) {
    prompt += `**ДАННЫЕ ЗАДАЧИ:**\n\n`

    for (const [tabName, tabData] of Object.entries(taskData.tabs)) {
      prompt += `--- ${tabName.toUpperCase()} ---\n`
      prompt += `${tabData.content || "Нет данных"}\n\n`
    }
  }

  prompt += `**ЗАДАНИЕ:** Проанализируйте задачу и дайте обоснованную оценку времени реализации.`

  return prompt
}